#include <stdio.h>
long int Num(int n);
int main()
{
    int n;
    scanf("%d", &n);
    printf(" %ld", Num(n));
    return 0;
}
long int Num(int n)
{
    if (n >= 1)
        return n*multiplyNumbers(n-1);
    else
        return 1;
}
